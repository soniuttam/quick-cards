import Foundation

enum AppConstants {
    static let notesWindowID = "notes"
    static let quickCaptureWindowID = "quick-capture"
    static let quickCaptureURL = URL(string: "quickcards://quick-capture")!
}
